[package]
name = "wt"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
inquire = "0.9"
fuzzy-matcher = "0.3"
toml = "1.1.2"
serde = { version = "1.0.228", features = ["derive"] }
